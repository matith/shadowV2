# 拾光 V2 · 今晚无人值守连续施工任务书

任务：开始拾光 V2 P0 无人值守连续施工。

这是今晚的正式执行授权。

仓库：
https://github.com/matith/shadowV2

本地：
D:\LIB\mimo\shadowV2

当前已接受的架构基线：
commit b167253
BBM canonical g000256

目标：

在不需要用户持续监管的情况下，依据当前 BBM 和 P0 Planning，连续完成 P0 代码实现、模块联调、模拟输入端到端测试、缺陷修复和回归。

最终停在：

READY_FOR_INDEPENDENT_AUDIT
 / 交付待独立审计

━━━━━━━━━━━━━━━━━━
一、执行权限
━━━━━━━━━━━━━━━━━━

本轮允许 Sol 自主：

- 阅读 BBM canonical
- 补齐 P0 execution closure
- 按黑盒逐个实现
- 调用/安排 Luna 执行具体编码
- 进行模块级测试
- 进行跨模块联调
- 使用模拟输入进行真实流程级测试
- Debug
- 修复 P0 范围内发现的问题
- 必要时修改 P0 范围内 BBM
- 更新 implementation link / verification / evidence
- 提交 Git commit
- 推送本轮成果
- 反复执行“实现 → 测试 → 修复 → 回归”

不需要因为普通 Bug、测试失败、局部实现选择而等待用户确认。

今晚希望的是连续工作，而不是每完成一个模块就暂停汇报。

━━━━━━━━━━━━━━━━━━
二、开工 Gate
━━━━━━━━━━━━━━━━━━

第一步只做快速核对，不重新审整个架构：

repo:
matith/shadowV2

local root:
D:\LIB\mimo\shadowV2

baseline:
b167253

canonical:
g000256 或其合法后继 generation

project UUID:
11dd729c-beaa-42cf-bad1-a06ab4f89e1b

确认没有绑定到其他 BBM 项目。

由于 Identity 原生硬门禁仍为：

ACCEPTED_BUT_NOT_IMPLEMENTED

本轮由 Sol 在开工时人工执行这一次 Identity Gate。

核对成功后继续施工，不需要再次向用户确认。

如果发现项目身份不匹配：
立即停止写入，这是本轮少数硬停止条件之一。

━━━━━━━━━━━━━━━━━━
三、先做一次极窄的 P0 Execution Closure
━━━━━━━━━━━━━━━━━━

不要再做产品架构大审计。

只根据：

- plan-p0-v2
- Scenario A
- Scenario C
- Scenario E
- X 系列 P0 相关验收
- 当前 68 个黑盒

确认 P0 真正参与端到端闭环的小黑盒是否全部进入 execution scope。

重点核一下目前可能遗漏但实际参与 P0 的模块，例如：

- model-adapter
- working-context-compiler
- ledger-router（若 P0 实际需要）
- raw-source-store
- delivery-router
- delayed-scheduled-runner
- table-projection / 当前 P0 Inspector 对应实现
- change-receipt
- dirty-tracker

这里只允许：

补 P0 execution binding / scope。

不要再扩展产品架构。

完成后立即进入代码施工。

━━━━━━━━━━━━━━━━━━
四、今晚施工优先级
━━━━━━━━━━━━━━━━━━

今晚的最低必须目标是：

P0 完整闭环可运行、可测试、可交付独立审计。

优先级严格如下：

P0 实现
→ P0 模块联调
→ Scenario A/C/E
→ P0 回归
→ P0 形成独立可审计 checkpoint
→ 如果时间、上下文和系统状态仍然充足，可以继续施工 P1
→ 不进入 P2

也就是说：

P0 是“必须完成项”。

P1 是“有余力继续做”。

不要为了抢做 P1，牺牲 P0 的完整性、测试、证据或可审计性。

P0 通过后先建立稳定 checkpoint；若剩余执行窗口充足，则允许自主进入 P1，并尽可能完成若干完整黑盒，但不得进入 P2。

P0 目标闭环：

模拟消息输入
→ normalize
→ Agent Turn
→ Working Context
→ Ledger Operations
→ State / Commit
→ Event
→ Matter
→ People
→ Reminder
→ Source
→ Capsule
→ Projection / Inspector
→ 后续模拟输入
→ Correction
→ 同一 Matter 更新
→ Reminder 到期
→ 模拟投递
→ Task Action
→ State / Commit 回写
→ 最终状态一致

重点验收：

Scenario A — 日常承诺
Scenario C — 多轮事项
Scenario E — 纠正

以及直接相关的：

- 幂等
- Source 不被 Correction 改写
- Commit 成功后才能声称成功
- Reminder 重启/恢复语义
- Capsule 增量更新
- 同一 Matter 不重复创建
- 人机看到的状态一致
- 关键写入具备证据/回执

━━━━━━━━━━━━━━━━━━
五、按黑盒施工，不按“看到什么就改什么”
━━━━━━━━━━━━━━━━━━

BBM 是本轮施工图。

每开始一个黑盒：

1. 读取该黑盒当前 Spec
2. 读取它的直接 Boundary / Relation
3. 读取与它相关的 P0 Acceptance
4. 实现
5. 跑模块级 deterministic test
6. 再接入相邻黑盒
7. 更新 implementation / verification evidence
8. 继续下一个

如果只是代码 Bug：

直接按 BBM 修代码。

如果发现：

“按当前 BBM 实现会导致产品用途错误”
“某个黑盒职责本身不成立”
“接口无法完成预期 Scenario”
“为了修 Bug 必须破坏 Boundary Contract”

则：

先检查 BBM。

如果确认是架构问题：

先做最小 BBM 修订
→ 说明为什么
→ 再改代码。

禁止：

为了通过一个测试写特殊 case，而实际上违背黑盒原设计。

同时也禁止借一个小 Bug 重新设计整个拾光。

━━━━━━━━━━━━━━━━━━
六、无人值守规则
━━━━━━━━━━━━━━━━━━

今晚普通问题不要停下来问用户。

以下情况自行处理：

- 测试失败
- 编译失败
- 类型错误
- schema 对不上
- 模块实现细节
- 数据库选择的局部实现
- mock / fake adapter
- 临时测试 fixture
- UI Inspector 的小调整
- P0 内部重构
- 非产品级代码组织选择

优先选择：

最简单、可验证、符合 BBM Contract 的实现。

不要为了未来 P2 设计复杂抽象。

真正需要停下来的只有：

1. 项目 Identity 不匹配
2. 必须改变用户已经确认的产品核心意图
3. 必须扩大到 P1/P2 才能继续
4. 需要真实外部高风险操作
5. 无法通过模拟替代且确实缺少用户提供的信息/凭证

若遇到非关键阻塞：

记录 blocker
→ 跳到独立可施工部分
→ 尽量继续推进。

━━━━━━━━━━━━━━━━━━
七、真实输入暂时全部允许模拟
━━━━━━━━━━━━━━━━━━

今晚不要求真实企业微信、真实手机通知、真实外部服务。

凡是外部输入/输出均可以使用：

Fake / Mock / Test Adapter

但注意：

模拟的是边界，不是业务逻辑。

例如：

真实用户消息：

“明天下午提醒我联系李经理，合同还没盖章。”

可以通过：

FakeInboundChannel

注入。

Reminder 可以使用：

FakeClock / controllable clock

把时间推进到明天下午。

真实通知可以替换为：

FakeDeliveryAdapter

并验证：

- 是否只发送一次
- payload 是否正确
- matter_ref 是否正确
- collapse/dedup 是否正确
- delivery receipt 是否生成

Projection 可以先使用：

Local Inspector / Test Projection

无需今晚接 WPS。

真实 Source 可以使用：

fixture text / fixture file

但必须经过真正的 Source Store / hash / provenance 路径。

也就是说：

可以模拟“世界”，
不能绕过拾光自己的黑盒。

━━━━━━━━━━━━━━━━━━
八、至少完成一套完整模拟剧本
━━━━━━━━━━━━━━━━━━

建议固定下面这套作为 nightly P0 integration fixture：

T0：

用户：
“明天下午提醒我联系李经理，合同还没盖章。”

期望：

- Event created
- Matter created/reused
- People 李经理
- Reminder created
- Source preserved
- Capsule generated
- Projection visible

T1：

用户：
“补充一下，是周三之前要确认。”

期望：

- 仍为同一 Matter
- 新 Event append
- 当前状态更新
- Capsule incrementally updated

T2：

用户：
“刚才说错了，不是周三，是周四。”

期望：

- Raw Source 均保留
- 旧 interpretation / state 被 Correction 替代
- Current State = 周四
- 后续 Context 不再使用周三

T3：

FakeClock 推进到 Reminder due。

期望：

- Reminder 只触发一次
- 走 Directive Policy
- 走 Delivery Router
- FakeDelivery 收到正确 payload
- 有可追踪 receipt

T4：

模拟用户点击：

“完成”

期望：

Task Action
→ Durable
→ State Commit
→ Matter 更新

不得由 Durable 直接写 Matter。

T5：

重启应用 / 重建 runtime session。

再次询问：

“李经理那个合同现在怎么样？”

期望：

无需聊天 Session，
仍能从 Canonical 恢复正确当前状态和必要证据。

━━━━━━━━━━━━━━━━━━
九、测试策略
━━━━━━━━━━━━━━━━━━

允许 Sol 无监管连续联调。

测试至少分三层：

A. 黑盒单元测试
验证每个 P0 小黑盒自己的 Contract。

B. Boundary / integration test
验证相邻黑盒真实传递的数据结构和失败语义。

C. Scenario test
验证 A/C/E 从用户输入到最终状态的端到端结果。

出现失败：

定位
→ 修复
→ 重跑局部
→ 重跑相关 Scenario

不要每修一个 Bug 就做一次全项目巨大回归。

阶段收口时再跑完整 P0 回归。

━━━━━━━━━━━━━━━━━━
十、允许独立审计，不要求 Sol 自己当最终审计者
━━━━━━━━━━━━━━━━━━

Sol 今晚负责：

规划收口
→ 实现
→ 联调
→ 自测
→ 修复
→ 整理证据

最终状态只允许写：

READY_FOR_INDEPENDENT_AUDIT

不要写：

FINAL PASS
FULLY ACCEPTED
PRODUCTION READY

最终接受权留给后续独立 Auditor / 用户。

如果有条件，可以在实现过程中使用 Luna 做局部代码审查或测试复核。

但最终独立审计应与本轮主执行分开。

━━━━━━━━━━━━━━━━━━
十一、Git 与证据
━━━━━━━━━━━━━━━━━━

按照仓库现有 Git 纪律工作。

建议以“可独立验证的黑盒/闭环”为合理提交粒度。

不要每改一行一个 commit，也不要整晚只有一个无法审阅的大提交。

每个阶段保留：

- 实现代码
- tests
- deterministic test output
- BBM implementation links
- verification evidence
- 必要的 BBM 修订
- 当前未解决 blocker

所有临时测试产物放临时目录，不污染 canonical。

阶段结束保持工作区干净并 push。

━━━━━━━━━━━━━━━━━━
十二、P0 完成后的继续施工规则
━━━━━━━━━━━━━━━━━━

当且仅当以下条件全部满足：

1. P0 Scenario A/C/E 已通过；
2. P0 相关 deterministic / integration / regression tests 已通过；
3. P0 当前代码处于可运行状态；
4. BBM implementation links / verification evidence 已更新；
5. 已形成一个清晰 Git checkpoint；
6. 当前没有 P0 blocker；
7. 继续施工不会要求重新设计已经接受的顶层架构；

则允许继续进入 P1。

进入 P1 不需要等待用户再次确认。

但 P1 必须继续遵守：

BBM → 黑盒 → Contract → 实现 → 测试 → 联调 → Evidence

不能变成自由发挥。

━━━━━━━━━━━━━━━━━━
十三、P1 的施工策略
━━━━━━━━━━━━━━━━━━

如果进入 P1，优先按照当前 canonical 中：

plan-p1-v2

施工。

优先实现能够自然延续 P0 的能力，例如：

- Matter 父子层级
- Tree Propagation
- Archive 最小召回
- Knowledge Ledger
- Product Ledger
- File Ledger
- 多 Ledger fan-out
- HOT/WARM/COLD 最小机制
- Backup / Migration 最小闭环

具体顺序由 Sol 根据：

- 当前代码依赖
- 黑盒依赖关系
- 实现成本
- 可测试性
- 与已完成 P0 的连续性

自行决定。

不要求今晚完成全部 P1。

宁可完成 2～3 个完整可验证的 P1 黑盒，
也不要铺开十几个半成品。

━━━━━━━━━━━━━━━━━━
十四、P1 施工必须保持阶段隔离
━━━━━━━━━━━━━━━━━━

P0 一旦通过，必须先形成明确 checkpoint。

例如：

P0_BASELINE_ACCEPTED_FOR_AUDIT
commit: xxxx

之后 P1 的代码继续建立在这个 checkpoint 上。

这样即使 P1 夜间施工失败：

- P0 仍然可独立运行；
- P0 仍然可以直接交给独立 Auditor；
- 不允许因为 P1 未完成导致 P0 无法启动或无法测试。

如果 P1 修改共享 Core：

必须确保 P0 regression 持续通过。

任何 P1 修改导致 P0 regression：

立即修复。

如果短时间无法修复：

回退到最近稳定状态，
不要把坏状态留给第二天。

━━━━━━━━━━━━━━━━━━
十五、P1 中发现架构问题怎么办
━━━━━━━━━━━━━━━━━━

仍然遵守 BBM 闭环：

代码问题
→ 按当前 BBM 修

模块能力问题
→ 查 BBM

架构/接口本身错误
→ 先最小修改 BBM
→ 再施工

但是：

不要因为进入 P1 再次展开一次全项目架构审计。

只处理当前正在施工黑盒所暴露出的真实问题。

禁止：

“既然到了 P1，顺便重新设计整个 Memory / Agent / Ledger 架构。”

━━━━━━━━━━━━━━━━━━
十六、P2 边界
━━━━━━━━━━━━━━━━━━

今晚不要自动进入 P2。

以下内容除非是 P0/P1 测试所需的 Fake Adapter，否则不正式施工：

- 企业微信真实接入
- 邮件真实接入
- OCR 完整产品能力
- 语音完整产品能力
- 多渠道产品化
- WPS 深度集成
- 外部 MCP 大规模接入
- Mem0 / Graphiti 正式集成
- Background Intelligence 完整实现
- 多模型复杂路由
- 复杂权限体系

这些可以留接口、写 observation，但不施工。

━━━━━━━━━━━━━━━━━━
十七、今晚停止条件
━━━━━━━━━━━━━━━━━━

以下情况之一满足即可停止。

A. P0 完成，但时间/上下文不足以安全进入 P1

→ P0 完整回归
→ 整理 evidence
→ push
→ READY_FOR_INDEPENDENT_AUDIT
→ STOP

B. P0 完成且继续施工了一部分 P1

→ 保证 P0 regression 仍 PASS
→ 对已完成 P1 黑盒分别测试
→ 不要求 P1 全部完成
→ 整理当前 P1 progress
→ push
→ READY_FOR_INDEPENDENT_AUDIT
→ STOP

C. P0 + P1 恰好全部完成

→ 做完整 P0 + P1 regression
→ 整理 evidence
→ push
→ READY_FOR_INDEPENDENT_AUDIT
→ STOP

D. 出现真正产品级阻塞

→ 保存最近稳定 checkpoint
→ 尽量完成其他独立工作
→ 记录 blocker
→ WAITING_USER_DECISION
→ STOP

不要为了“必须一直跑到早上”而制造工作。

如果已经没有当前阶段有价值、可安全继续的任务，
可以提前结束。

━━━━━━━━━━━━━━━━━━
十八、第二天的状态必须区分
━━━━━━━━━━━━━━━━━━

最终报告必须分别写：

P0 STATUS:
PASS / PARTIAL / BLOCKED

P1 STATUS:
NOT_STARTED / IN_PROGRESS / PARTIAL / PASS

不得因为做了一部分 P1，就把整体写成：

“P1 完成”。

同样，不得因为 P1 失败，覆盖已经稳定完成的 P0。

最终总状态仍然只允许：

READY_FOR_INDEPENDENT_AUDIT

或

WAITING_USER_DECISION

━━━━━━━━━━━━━━━━━━
十九、明早给用户的最终汇报只需要这些
━━━━━━━━━━━━━━━━━━

1. BASELINE / FINAL HEAD
2. P0 实际实现了哪些黑盒
3. 哪些 BBM 在施工中发生过修订，以及为什么
4. Scenario A/C/E 的结果
5. 单元 / integration / regression 测试结果
6. 模拟了哪些外部输入/输出
7. 哪些真实集成仍未测试
8. 当前 blocker / known limitation
9. Independent Audit 应重点检查什么
10. 最终状态：

READY_FOR_INDEPENDENT_AUDIT

或

WAITING_USER_DECISION

P0 STATUS:
PASS / PARTIAL / BLOCKED

P1 STATUS:
NOT_STARTED / IN_PROGRESS / PARTIAL / PASS

今晚不要持续向用户发送过程汇报。

直接执行，直到达到上述停止条件。
